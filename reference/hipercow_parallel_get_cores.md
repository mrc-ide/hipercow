# Get number of cores

Lookup number of cores allocated to the task

## Usage

``` r
hipercow_parallel_get_cores()
```

## Value

The number of cores a cluster has allocated to your task. This will be
less than or equal to the number of cores on the cluster node running
your task.
