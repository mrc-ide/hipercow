hipercow_drivers <- function() {
  list(
    "windows"      = c("hipercow.windows", "hipercow_driver_windows"),
    "dide-windows" = c("hipercow.windows", "hipercow_driver_windows"),
    "example"      = c("hipercow", "example_driver"))
}
