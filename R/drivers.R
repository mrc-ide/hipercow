hipercow_drivers <- function() {
  list(
    "windows"      = c("hipercow.dide", "hipercow_driver_windows"),
    "dide-windows" = c("hipercow.dide", "hipercow_driver_windows"),
    "dide-linux"   = c("hipercow.dide", "hipercow_driver_linux"),
    "example"      = c("hipercow", "example_driver"))
}
